@model IEnumerable<Incedo_Octavius_Demo_2.Models.BusinessUserDegreeModel>

@{
    ViewBag.Title = "Index";
}

<h2>Degree Business Rules</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Degree_Map)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Degree_Master)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Degree_Map)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Degree_Master)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MapID }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.MapID }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.MapID })
        </td>
    </tr>
}

</table>
